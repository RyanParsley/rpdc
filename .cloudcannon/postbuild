# CloudCannon Postbuild Hook - DISABLED
# POSSE syndication now runs automatically during Astro build via integration
# This hook is kept for reference but should be removed or disabled

echo "ℹ️  POSSE syndication now runs automatically during Astro build"
echo "ℹ️  This CloudCannon postbuild hook is no longer needed"
echo "ℹ️  To disable: rename this file or remove it from .cloudcannon/postbuild"

# Uncomment the lines below if you want to re-enable the old standalone script
# echo "🔄 Starting POSSE syndication (legacy mode)..."
# ... rest of the original script ...
